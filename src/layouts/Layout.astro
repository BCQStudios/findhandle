<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta
			name="description"
			content="Check if a handle is available on YouTube, TikTok, X, Facebook, and Instagram."
		/>
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>Findhandle</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	:root {
		--bg: #070708;
		--fg: #f4f4f5;
		--muted: #9a9aa3;
		--line: #26262b;
		--card: #101012;
		--bar: #ffffff;
		--ink: #0b0b0d;
		--taken: #ff2a2a;
		--available: #17d36a;
		--unknown: #d0d0d6;
		--youtube: #ff0000;
		--tiktok: #fe2c55;
		--tiktok-cyan: #25f4ee;
		--x: #ffffff;
		--facebook: #1877f2;
		--instagram: #ff0069;
	}

	*,
	*::before,
	*::after {
		box-sizing: border-box;
	}

	html,
	body {
		margin: 0;
		min-height: 100%;
	}

	body {
		background: var(--bg);
		color: var(--fg);
		font-family: "Segoe UI", Inter, ui-sans-serif, system-ui, sans-serif;
		-webkit-font-smoothing: antialiased;
	}

	button,
	input {
		font: inherit;
	}
</style>
